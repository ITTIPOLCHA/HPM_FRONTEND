{
  "$schema": "https://aka.ms/azure/static-web-apps-cli/schema",
  "configurations": {
    "hpm-frontend": {
      "appLocation": "src",
      "outputLocation": "build",
      "appBuildCommand": "npm install --legacy-peer-deps && npm run build:serve:dev",
      "run": "npm run start:dev",
      "appDevserverUrl": "http://localhost:3000"
    }
  }
}